{
  "id": "tasks-claimable",
  "name": "Claimable Tasks",
  "category": "data",
  "description": "Task claiming system with admin grading workflow",
  "details": "A task system where users can claim unclaimed tasks and submit work for admin review. Demonstrates 'unclaimed-or-own' permission level, writableFields for restricting which fields non-admins can update, and timestampTrigger for automatic timestamp fields.",
  "files": [
    { "src": "tasks-schema.ts", "dest": "src/schemas/tasks-schema.ts" },
    { "src": "tasks-constants.ts", "dest": "src/constants/tasks-constants.ts" },
    { "src": "TasksPage.tsx", "dest": "src/pages/TasksPage.tsx" }
  ],
  "schema": {
    "exportName": "challengesSchema",
    "importPath": "./schemas/tasks-schema",
    "spreadOperator": false
  },
  "route": {
    "path": "/tasks",
    "component": "TasksPage",
    "importPath": "./pages/TasksPage",
    "protected": false
  },
  "requires": ["shared-ui"],
  "instructions": [
    "Add to schemas.ts: import { challengesSchema } from './schemas/tasks-schema'",
    "Add to schemas array: challengesSchema",
    "Add to App.tsx: import TasksPage from './pages/TasksPage'",
    "Add route: <Route path=\"/tasks\" element={<TasksPage />} />",
    "Add navigation link for '/tasks'"
  ],
  "patterns": [
    "update: 'unclaimed-or-own' → claim unclaimed OR update own",
    "writableFields: ['claimedById', 'submitted'] → restrict editable fields",
    "timestampTrigger: { field: 'claimedById' } → auto-timestamp",
    "Admin grading: check user.role === 'admin'"
  ]
}
